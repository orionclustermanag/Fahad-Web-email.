# Example environment variables (optional)
# RAILWAY can set these in project settings if you change the server to use them.
# IMAP_HOST=imap.gmail.com
# IMAP_PORT=993
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
